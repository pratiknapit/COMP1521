// 0 ^ 1 = 1
// 1 ^ 1 = 0

// See above that XOR can be used to flip original bits.
// Can be used to encode messages by flipping select bits with XOR and a mask. 
// And then decode by unflipping bits again with XOR.